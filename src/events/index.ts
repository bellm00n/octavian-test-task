import EventObserver from '../utils/EventObserver';

export const tickEvent = new EventObserver();
export const spinButtonClickEvent = new EventObserver();
export const spinStartEvent = new EventObserver();
